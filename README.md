# Unsafe flask website login
This is not a safe login system
## Prerequisites
- Python
- pip

### Gudie
### Gudie
- cd Secure-App-Project
- python -m venv venv (if needed)
- pip install flask
- pip install sqlite3 (if not installed by default)
- python run.py